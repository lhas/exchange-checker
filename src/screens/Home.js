import React from 'react';
import {
  Text,
  View
} from 'react-native';

const HomeScreen = ({ navigation }) => (
  <View style={{ flex: 1 }}>
    <Text>Home Screen</Text>
  </View>
);

export default HomeScreen;
